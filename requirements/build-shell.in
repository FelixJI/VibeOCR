# CI 打包壳的最小直接依赖集
#
# 这些是 WinUI release staging 与迁移质量门禁所需的最小第三方包。
# paddle/torch/模型依赖仍由复用的便携 Python 运行时提供，不在 CI 安装。
#
# 生成 lock 文件：
#   uv pip compile requirements/build-shell.in --generate-hashes -o requirements/build-shell.lock
# CI 安装：
#   python -m pip install --require-hashes -r requirements/build-shell.lock
#
# 正式 WinUI 主程序不再由 PyInstaller 打包；仅纯 stdlib 的 updater.exe 仍用
# PyInstaller onefile 生成，以便旧版向 WinUI 切换时由新代码完成替换。

pyinstaller>=6.6
pyside6>=6.8
pillow
numpy
markdown
qasync
httpx
fastapi
uvicorn
pymupdf
fonttools
qrcode[pil]
python-barcode
opencv-contrib-python
pyzbar
pydantic
openpyxl
python-docx

# Release CI 同时执行 WorkerHost v1 双语言契约门禁。
pytest==9.1.1
pytest-cov>=7.1.0
pytest-asyncio>=1.2.0
pytest-qt==4.5.0
python-multipart>=0.0.22
jsonschema==4.25.1
ruff>=0.15.13
pyright>=1.1.409
