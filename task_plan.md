# 任务计划：修复 WinUI 开发启动与核心界面

## 目标

让 WinUI 在开发运行时可靠拉起共享后端，并以 PySide6 版本的信息架构为基准，优先把“单次截图”页面恢复到可用、清晰、可验证的状态。

## 阶段

### 阶段 A：现状与差异审计
- [x] 定位 WinUI 开发启动入口、后端会话生命周期和现有测试
- [x] 对照 PySide6 与 WinUI 的导航、页面和单次截图交互
- [x] 记录根因、可复用组件和受影响文件
- **状态：完成**

### 阶段 B：开发启动链路修复
- [x] 修复调试/开发运行时的后端自动启动
- [x] 补齐错误提示、退出清理和自动化测试
- **状态：完成（待有 SDK 的环境执行 .NET 测试）**

### 阶段 C：单次截图页重构
- [x] 以 PySide6 工作流为基准确定布局和状态
- [x] 重构 WinUI 页面及 ViewModel/资源，消除杂乱和不可用交互
- [x] 保证空态、处理中、成功、失败状态可理解
- **状态：完成（真实 WinUI 运行仍需有 SDK 的机器验证）**

### 阶段 D：其余界面一致性收敛
- [x] 修复能安全复用的全局导航、窗口尺寸与首屏问题
- [x] 对超出本轮范围的高成本差异形成明确清单
- **状态：完成**

### 阶段 E：验证与交付
- [x] 运行可用的 .NET/Python 测试与静态检查
- [x] 检查 diff，记录本机无法完成的真实 WinUI 验证
- **状态：完成**

## 约束与决策

| 项目 | 决策 |
|---|---|
| 既有改动 | 保留两个 `packages.lock.json` 的用户改动，除非任务确实需要更新 |
| 设计方向 | 工具型、克制、信息层级明确；优先可用性和与 PySide6 工作流一致 |
| 实施顺序 | 先修启动链路，再集中修单次截图页，最后处理可复用的全局一致性 |

## 错误记录

| 错误 | 处理 |
|---|---|
| 系统未注册 `python` 命令，会话恢复脚本首次运行失败 | 改用 Codex 工作区捆绑的 Python，恢复成功 |
| 本机只有 x86 `dotnet` host，没有任何 SDK；项目要求 10.0.302 | 无法本机构建 .NET，继续做静态审计与 Python/文本级验证，并在交付中明确标注 |
| 一次 `rg` 验证命令的正则引号被 PowerShell 截断 | 改为多个 `-e` 固定模式，复查成功 |
