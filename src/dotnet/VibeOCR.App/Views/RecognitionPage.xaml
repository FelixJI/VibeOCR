<Page
    x:Class="VibeOCR.App.Views.RecognitionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <TextBlock FontSize="28" FontWeight="SemiBold" Text="单图识别" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Click="OnFileClicked" Content="打开图片" /><Button Click="OnClipboardClicked" Content="从剪贴板" />
            <Button Click="OnScreenshotClicked" Content="截取桌面" /><Button Click="OnCancelClicked" Content="取消" />
            <ProgressRing Width="24" Height="24" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />
        </StackPanel>
        <Border Grid.Row="2" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
            <StackPanel Orientation="Horizontal" Spacing="24"><TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}" /><TextBlock x:Name="PreviewBridgeStatus" Text="Web 预览正在加载" /></StackPanel>
        </Border>
        <Grid Grid.Row="3" ColumnSpacing="12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <WebView2 x:Name="PreviewWebView" />
            <Border Grid.Column="1" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                <StackPanel Spacing="8"><TextBlock FontWeight="SemiBold" Text="编辑与结果已移入安全预览区" /><TextBlock Text="支持结构化文本、表格、公式、标注与撤销/重做；此处保留状态提示。" TextWrapping="Wrap" /></StackPanel>
            </Border>
            <Border x:Name="PreviewErrorPanel" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Visibility="Collapsed"><TextBlock x:Name="PreviewError" Text="预览资源未就绪" /></Border>
        </Grid>
    </Grid>
</Page>
